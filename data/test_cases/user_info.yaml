- name: "获取用户服务信息"
  description: "测试获取用户服务信息功能"
  request:
    method: GET
    data_type: "form"
    url: "/eviapp/v3/AppV3/srvAssistant/servInfo"
    headers:
      User-Agent: "Apache-HttpClient/4.5.7"
      Accept-Encoding: "gzip, deflate"
      accept: "application/json"
      Connection: "keep-alive"
      Content-Type: "application/x-www-form-urlencoded"
      charset: "UTF-8"
#      Content-Length: "0"

    data:
#      loginname: "${username}" # 从env_config.yaml获取
#      password: "${password}"
      userCode: "${X-Auth-Code}"
#      password: "334420"
#      type: "1"
    extract:
      X-Auth-Code: jsonpath:$.data.user_uniquecode
      user_id: jsonpath:$.data.user_id

#- name: "获取用户信息"
#  description: "测试获取用户信息功能"
#  request:
#    method: GET
#    url: /api/user/info
#    headers:
#      Content-Type: application/json
#      Authorization: Bearer ${token}  # 使用登录接口提取的token
#    extract:
#      username: jsonpath:$.data.username
#      email: jsonpath:$.data.email
